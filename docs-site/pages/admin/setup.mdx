---
title: Setup & Configuration
description: Getting started with Pariflow administration
---

# Setup & Configuration

This guide covers the basic setup for running Pariflow locally for development.

## Prerequisites

- Node.js 18+ installed
- PostgreSQL database
- Redis server (optional but recommended)
- Environment variables configured

## Initial Setup

### 1. Install Dependencies

```bash
npm install
```

### 2. Configure Environment Variables

Create a `.env` file in the root directory with the required configuration. Contact your team administrator for the specific values needed.

Required environment variables:
- Database connection string
- Redis connection (optional)
- Authentication secrets
- Third-party API credentials (if using external integrations)

### 3. Database Setup

```bash
# Run migrations
npx prisma migrate dev

# Generate Prisma Client
npx prisma generate

# (Optional) Seed database
npm run seed
```

### 4. Start Development Server

```bash
npm run dev
```

The application will be available at `http://localhost:3000`

## Configuration

### Database Configuration

Edit `prisma/schema.prisma` to configure database connection and models.

### Redis Configuration

Redis is used for:
- Caching frequently accessed data
- Pub/sub for real-time updates
- Rate limiting

### WebSocket Configuration

WebSocket server runs on a configurable port for real-time updates.

## Feature Setup

### Enable Hedging

See [Hedging System Guide](/admin/hedging) for complete hedging setup.

### Enable Telegram Bot

1. Create Telegram bot via BotFather
2. Configure webhook URL
3. Set up notification handlers

## Production Deployment

### Vercel Deployment

1. Connect repository to Vercel
2. Configure environment variables in Vercel dashboard
3. Deploy

### Custom Server

1. Build application:
   ```bash
   npm run build
   ```

2. Start production server:
   ```bash
   npm start
   ```

3. Configure reverse proxy (nginx, etc.)

## Monitoring

### Health Checks

Monitor system health at `/api/health` endpoint.

### Logging

- Application logs: Check server logs
- Error tracking: Integrated monitoring
- Analytics: Vercel Analytics

## Troubleshooting

### Database Connection Issues

- Verify database URL is correct
- Check PostgreSQL is running
- Verify network connectivity

### Redis Connection Issues

- Verify Redis URL is correct
- Check Redis is running
- System works without Redis (degraded mode)

### Build Errors

- Clear `.next` directory: `rm -rf .next`
- Reinstall dependencies: `rm -rf node_modules && npm install`
- Check Node.js version: `node --version`

## Next Steps

- [Hedging System Setup](/admin/hedging)
- [Architecture Documentation](/architecture/overview)

---

For more details, see the [Quick Start Guide](/getting-started/quick-start) or contact support.
